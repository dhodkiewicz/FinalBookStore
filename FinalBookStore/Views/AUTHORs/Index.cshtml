@model IEnumerable<FinalBookStore.Models.EntityFramework.AUTHOR>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AUTHOR_LAST)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AUTHOR_FIRST)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AUTHOR_LAST)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AUTHOR_FIRST)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AUTHOR_NUM }) |
            @Html.ActionLink("Details", "Details", new { id=item.AUTHOR_NUM }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AUTHOR_NUM })
        </td>
    </tr>
}

</table>
